<!--

    Copyright The WildFly Authors
    SPDX-License-Identifier: Apache-2.0

-->
<subsystem xmlns="urn:wildfly:grpc:preview:1.0" flow-control-window="${test.flow-control-window:3}"
    handshake-timeout="${test.handshake-timeout:5}" initial-flow-control-window="${test.initial-flow-control-window:7}"
    keep-alive-time="${test.keep-alive-time:11}" keep-alive-timeout="${test.keep-alive-timeout:13}"
    max-concurrent-calls-per-connection="${test.max-concurrent-calls-per-connection:17}"
    max-connection-age="${test.max-connection-age:19}" max-connection-age-grace="${test.max-connection-age-grace:23}"
    max-connection-idle="${test.max-connection-idle:29}" max-inbound-message-size="${test.max-inbound-message-size:31}"
    max-inbound-metadata-size="${test.max-inbound-metadata-size:37}"
    permit-keep-alive-time="${test.permit-keep-alive-time:39}"
    permit-keep-alive-without-calls="${test.permit-keep-alive-without-calls:true}"
    protocol-provider="${test.protocol-provider:bbb}" session-cache-size="${test.session-cache-size:47}"
    session-timeout="${test.session-timeout:51}" shutdown-timeout="${test.shutdown-timeout:53}"
    start-tls="${test.start-tls:true}" socket-binding="grpc" />